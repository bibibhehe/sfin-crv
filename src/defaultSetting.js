const defaultSettings = {
  pageSize: 10,
  historyDays: 30,
  inactivityTimeout: 36000000, //1800 second
  monitorEvents: ['scroll', 'click', 'contextmenu', 'keydown', 'mousemove']
};

export default defaultSettings;
